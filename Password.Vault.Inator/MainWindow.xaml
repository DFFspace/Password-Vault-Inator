<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="Password.Vault.Inator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dev="using:DevWinUI"
    xmlns:local="using:Password.Vault.Inator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <NavigationView
            x:Name="NavigationViewControl"
            Grid.Row="1"
            HighContrastAdjustment="None"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            ItemInvoked="NavigationViewControl_ItemInvoked"
            Style="{StaticResource NavigationViewStoreStyle}">
            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="Home"
                    Icon="Home"
                    Tag="Password.Vault.Inator.Pages.HomePage" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem
                    Content="Feedback"
                    Icon="{dev:FontIcon GlyphName=LockFeedback}"
                    IsEnabled="False"
                    SelectsOnInvoked="False"
                    Tag="Contribute" />
                <NavigationViewItem
                    Content="Settings"
                    Icon="Setting"
                    IsEnabled="False"
                    Tag="Password.Vault.Inator.Pages.SettingsPage" />
            </NavigationView.FooterMenuItems>
            <NavigationView.Content>
                <Frame x:Name="ContentFrame" Navigated="ContentFrame_Navigated" />
            </NavigationView.Content>
        </NavigationView>

        <!--  Title Bar  -->
        <TitleBar
            x:Name="ApplicationTitleBar"
            Title="Password Vault-Inator"
            BackRequested="TitleBar_BackRequested"
            IsBackButtonVisible="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/strongbox.ico" />
            </TitleBar.IconSource>
        </TitleBar>
    </Grid>
</winuiex:WindowEx>
